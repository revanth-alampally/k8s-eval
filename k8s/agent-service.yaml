apiVersion: v1
kind: Service
metadata:
  name: k8s-ops-agent
  namespace: ai-agent-demo
  labels:
    app.kubernetes.io/name: k8s-ops-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k8s-ops-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
